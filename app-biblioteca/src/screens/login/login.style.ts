import { StyleSheet } from "react-native";

const loginStyle = StyleSheet.create({
   container:{
      flex:0,
   },
   input:{
      marginTop:15,
      marginBottom:15,
   }
});

export default loginStyle;